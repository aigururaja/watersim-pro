# The Android app is a Trusted Web Activity generated by Bubblewrap from
# twa-manifest.json. Everything else here is generated or built output and is
# recreated with `npx @bubblewrap/cli update` and `build` (see the runbook).
*
!.gitignore
!twa-manifest.json
