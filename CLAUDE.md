# watersim-pro repository

The application is not at this root. It lives in
`Documents/watersim-pro/watersim_session16/` (an npm-workspaces monorepo with
`backend/` and `frontend/`). The sibling `Documents/watersim-pro/cmms-hydrogen/`
is the CMMS it integrates with.

Read `Documents/watersim-pro/watersim_session16/CLAUDE.md` before doing
anything: it holds the project's settled decisions (ISA-5.1 tags only, SQLite
only, monitoring vs twin projects), the production server, the shared-checkout
rules, the Windows and deployment traps, and the current status.
